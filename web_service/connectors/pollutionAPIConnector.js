module.exports = () => {
  return {
    async getPolutionIndice({ sensors }) {

      // Calculate the polution indice between 0 and 1 based on API data (fine dust, pollution)

      return 0.8
    }
  }
}
